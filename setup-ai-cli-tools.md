# 远程 Linux 机器一键配置 AI CLI 工具 🚀

## 前言

想在树莓派或其他 Linux 服务器上使用 Claude Code、Qoder CLI、CodeBuddy？手动安装配置太麻烦？

这篇文章分享一个自动化脚本，**一条命令搞定**三个 AI CLI 工具的安装和配置，还能自动设置智能别名，让你用起来更方便！

## 适用场景

- 🍓 **树莓派** - 打造你的 AI 开发环境
- 🖥️ **远程 Linux 服务器** - 批量配置多台机器
- 🐧 **WSL / 虚拟机** - 快速搭建开发环境
- 🔧 **个人 Linux 工作站** - 自动化配置工具链

## 支持的工具

| 工具 | 功能 | 安装方式 |
|------|------|----------|
| **Claude Code** | Anthropic 官方 CLI，强大的 AI 编程助手 | npm 全局安装 |
| **Qoder CLI** | 智能体编程平台，支持代码审查和自动化 | curl 安装脚本 |
| **CodeBuddy** | 腾讯云代码助手，深度代码库感知 | npm 全局安装 |

## 快速开始

### 前置要求

- SSH 访问权限到目标 Linux 机器
- 目标机器需要有 `sudo` 权限
- 目标机器需联网

### 一键安装

```bash
# 1. 克隆本仓库（如果还没有）
git clone https://github.com/your-username/AI-Notes.git
cd AI-Notes

# 2. 运行脚本
./setup-mirrors.sh user@your-server-ip

# 3. 在菜单中选择 4 和 9
# 4 - 安装 Node.js 和配置 NPM 镜像源
# 9 - 安装 AI CLI 工具
```

**快速下载（单个脚本）**：
```bash
# 如果只需要脚本，可以直接下载
curl -O https://raw.githubusercontent.com/your-username/AI-Notes/main/setup-mirrors.sh
chmod +x setup-mirrors.sh
./setup-mirrors.sh user@your-server-ip
```

### 完整演示

```bash
$ ./setup-mirrors.sh user@192.168.1.100

========================================
  远程 Linux 机器自动化配置工具
  目标: user@192.168.1.100
========================================

[准备] 测试 SSH 连接...
✅ SSH 连接成功

[信息] 获取远程主机信息...
主机名: my-server
系统: Debian GNU/Linux 13 (trixie)

请选择要执行的配置步骤（多选，用空格分隔）：

  1) SSH 免密登录配置 (ssh-copy-id)
  2) 安装 zsh 和 oh-my-zsh
  3) 配置 APT 镜像源（清华大学）
  4) 配置 NPM 镜像源（淘宝）
  5) 配置 PIP 镜像源（清华大学）
  6) 安装常用工具 (vim, curl, wget, git, htop)
  7) 配置 Git 用户信息
  8) 设置时区为 Asia/Shanghai
  9) 安装 AI CLI 工具 (Claude Code, Qoder CLI, CodeBuddy)

  a) 全部执行
  q) 退出

请输入选项（如: 1 3 4 或 a）: 4 9

# 然后脚本会自动：
# 1. 安装 Node.js v24.11.1
# 2. 配置 NPM 淘宝镜像
# 3. 安装 Claude Code v2.0.65
# 4. 安装 Qoder CLI v0.1.16
# 5. 安装 CodeBuddy v2.18.0
# 6. 配置智能别名到 ~/.zshrc
```

## 自动配置的智能别名

脚本会自动在 `~/.zshrc` 或 `~/.bashrc` 中添加以下配置：

### 1. Claude Code 智能启动

```bash
# 使用 AI 命令，自动继续上次会话
AI

# 或者强制开启新会话
AI -n
AI --new
```

**原理**：
```bash
function AI() {
    if [[ "$1" == "-n" ]] || [[ "$1" == "--new" ]]; then
        shift
        command claude "$@"
    else
        command claude --continue 2>/dev/null || command claude "$@"
    fi
}
```

### 2. Qoder CLI 智能启动

```bash
# 使用 qo 命令，自动继续上次会话
qo

# 或者强制开启新会话
qo -n
qo --new
```

### 3. CodeBuddy 智能启动

```bash
# 使用 cb 命令，自动继续上次会话
cb

# 或者强制开启新会话
cb -n
cb --new
```

## 配置后的使用

安装完成后，重新登录或运行：

```bash
source ~/.zshrc
```

然后就可以直接使用了：

```bash
# Claude Code
AI "帮我写个 Python 脚本"
AI -n "新会话：分析这段代码"

# Qoder CLI
qo "审查这个 PR"
qo -n "新会话：生成单元测试"

# CodeBuddy
cb "优化这个函数"
cb -n "新会话：重构代码"
```

## 智能别名的优势

### 传统方式
```bash
# 每次都要输入完整命令
claude --continue "问题"

# 或者手动判断要不要继续
claude "问题"  # 会创建新会话
```

### 智能别名方式
```bash
# 默认就是智能的，会尝试继续会话
AI "问题"

# 需要新会话时才指定
AI -n "问题"
```

**好处**：
- ✅ 默认行为更智能（自动继续会话）
- ✅ 节省 token（不用重复上下文）
- ✅ 命令更短（`AI` vs `claude --continue`）
- ✅ 统一的使用习惯

## 配置详解

### 自动安装流程

1. **检查 Node.js**
   - 检测是否已安装
   - 检查版本是否满足要求（CodeBuddy 推荐 22+）

2. **安装 Claude Code**
   ```bash
   sudo npm install -g @anthropic-ai/claude-code
   ```

3. **安装 Qoder CLI**
   ```bash
   curl -fsSL https://qoder.com/install | bash
   ```

4. **安装 CodeBuddy**
   ```bash
   sudo npm install -g @tencent-ai/codebuddy-code
   ```

5. **验证安装**
   - 检查命令是否可用
   - 显示版本信息
   - 显示安装路径

6. **配置智能别名**
   - 自动检测 shell 类型（zsh/bash）
   - 写入对应配置文件
   - 防止重复配置

### 防重复配置

脚本会检查配置文件中是否已存在标记：

```bash
if grep -q "# AI CLI Tools Aliases" "$SHELL_RC" 2>/dev/null; then
    echo "✓ 智能别名已配置"
else
    # 添加配置
fi
```

**好处**：
- 可以多次运行脚本而不会重复添加配置
- 安全可靠

## 批量配置多台机器

如果你有多台树莓派或服务器，可以这样批量配置：

```bash
#!/bin/bash
# batch-setup.sh

SERVERS=(
    "pi@192.168.1.10"
    "pi@192.168.1.11"
    "pi@192.168.1.12"
)

for server in "${SERVERS[@]}"; do
    echo "正在配置 $server ..."
    echo "4 9" | ./setup-mirrors.sh "$server"
    echo "✅ $server 配置完成"
    echo ""
done
```

## 常见问题

### Q: 安装失败怎么办？

**A**: 检查以下几点：
1. Node.js 是否安装成功（运行 `node --version`）
2. 网络连接是否正常
3. 是否有 sudo 权限
4. 查看详细错误信息

### Q: 智能别名不生效？

**A**: 需要重新加载配置：
```bash
# 方法1：重新登录
exit
ssh user@host

# 方法2：手动加载
source ~/.zshrc
```

### Q: 如何只安装其中一个工具？

**A**: 目前脚本会安装全部三个工具。如果只需要某个工具，可以手动安装：

```bash
# 只安装 Claude Code
sudo npm install -g @anthropic-ai/claude-code

# 只安装 Qoder CLI
curl -fsSL https://qoder.com/install | bash

# 只安装 CodeBuddy
sudo npm install -g @tencent-ai/codebuddy-code
```

### Q: Claude Code 需要 API Key 吗？

**A**: 是的，Claude Code 需要配置 `ANTHROPIC_AUTH_TOKEN`：

```bash
# 添加到 ~/.zshrc 或 ~/.bashrc
echo 'export ANTHROPIC_AUTH_TOKEN="sk-your-key-here"' >> ~/.zshrc
source ~/.zshrc
```

参考：[Claude Code 安装指南](./claude-installation-guide.md)

### Q: 在国内网络环境下安装慢怎么办？

**A**: 脚本已经配置了国内镜像源：
- Node.js: nodesource 官方源
- NPM: 淘宝镜像 (registry.npmmirror.com)
- Qoder CLI: 官方 CDN（国内可直连）

如果还是很慢，可以考虑使用代理或选择网络较好的时间段安装。

## 卸载

如果需要卸载这些工具：

```bash
# 卸载 Claude Code
sudo npm uninstall -g @anthropic-ai/claude-code

# 卸载 Qoder CLI
rm -rf ~/.qoder
rm ~/.local/bin/qodercli

# 卸载 CodeBuddy
sudo npm uninstall -g @tencent-ai/codebuddy-code

# 删除配置（可选）
# 手动编辑 ~/.zshrc，删除 "# AI CLI Tools Aliases" 部分
```

## 进阶使用

### 与其他工具配合

这些 AI CLI 工具可以与其他开发工具很好地配合：

```bash
# 用 Claude 分析 git diff
git diff | AI "解释这些改动"

# 用 Qoder 审查代码
qo "审查 src/main.py 的安全性"

# 用 CodeBuddy 生成测试
cb "为 utils.js 生成单元测试"
```

### 自定义配置

你可以在智能别名的基础上进一步自定义：

```bash
# 添加到 ~/.zshrc

# 快速代码审查
alias review='qo "审查当前 git diff 的代码"'

# 快速 debug
alias debug='AI "分析这个错误，给出修复建议："'

# 快速生成测试
alias gentest='cb "生成单元测试："'
```

## 脚本源码

完整脚本就在本仓库：
- GitHub: [setup-mirrors.sh](./setup-mirrors.sh)
- 可以直接下载使用，或者克隆整个仓库

## 相关文章

- [Claude Code 注册与安装指南](./claude-installation-guide.md) - 详细的安装配置教程
- [自动恢复会话配置](./claude-auto-resume-session.md) - 会话管理技巧
- [Claude Code 省钱小技巧](./claude-save-tokens-tips.md) - 优化 token 使用

## 总结

通过这个自动化脚本，你可以：

✅ **一键安装** - 无需手动逐个安装工具
✅ **自动配置** - 智能别名自动添加
✅ **批量部署** - 轻松配置多台机器
✅ **开箱即用** - 安装完立即可用

希望这个工具能帮你快速搭建 AI 开发环境，让 AI 工具真正成为你的编程助手！

---

**测试环境**：
- ✅ Raspberry Pi 5 (Debian 13)
- ✅ Ubuntu 22.04 LTS
- ✅ Debian 12

**最后更新**: 2025-12-12
